var bunny = 30;
var month = 1;

  while (month <= 6) {
    console.log("In the " + month +". month there are " + bunny + " bunny.");
    bunny *= 2;
    month += 1;
  }
  while (month <=12) {
    console.log("In the " + month +". month there are " + bunny + " bunny.");
    bunny *= 3;
    month += 1;
  }